# 2026-08-01

- 在独立分支 `feat/home-density-documentation` 完成首页信息密度切换增量 PRD：三档面向用户命名为「沉浸／标准／速览」，默认「标准」；信息密度与列表字号保持独立，三档共享“今日刊”品牌和内容顺序。文档覆盖双入口切换、即时重排、本机持久化、浏览锚点、P0/P1/P2、验收矩阵、指标、隐私、风险及待决问题，路径为 `docs/features/home-density/product-requirements.md`；同时更新 `docs/README.md` 文档入口。
- 经逐项设计确认，在 `feat/home-density-prototype` 完成首页三档可交互 HTML 原型与 UI 规格：沉浸档为 16:9 大图/抽象版纹、标题+摘要+指标；标准档为右侧 72pt 图、标题+摘要+指标；速览档为纯双行标题。首页保留 AI「知」并新增更多按钮，底部半屏面板即时预览、自动保存、保持首篇可见文章；覆盖浅深色、iPhone 402pt 与 iPad 可用宽度 ≥700pt 的沉浸双列。文件位于 `docs/design/home-density/interactive-prototype.html` 和 `design-spec.md`。
- 在 `feat/home-density-implementation` 完成首页三档 SwiftUI 实现：新增 `HomeInformationDensity` 持久化模型和可复用选择器，首页改为支持沉浸/标准/速览、宽度 ≥700pt 的沉浸双列、抽象报刊版纹、首页半屏切换与设置入口，并保留刷新、历史分页、详情和不感兴趣操作；新增模型单元测试与首页/设置 UI 用例。已通过 `xcodegen`、`swiftc -parse`、`git diff --check`；完整 `xcodebuild` 因当前 Xcode 27 Beta 在 SwiftPM 依赖解析阶段触发 `sandbox-exec: sandbox_apply: Operation not permitted` 而未进入源码编译。
