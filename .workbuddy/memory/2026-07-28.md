# 2026-07-28

- 更新“我”页面三套 UI 设计稿：在顶部新增纸上书房、日光收纳、静谧成长三台手机同屏对比区，统一尺寸和视觉基线；宽屏并排展示，窄屏支持横向滑动与吸附，下方仍保留各方案详细设计说明及决策矩阵。
- 完整 Xcode 27 Beta 4 与 iOS 27 Runtime 已配置可用；针对阅读位置、进度和返回顶部功能完成真实构建验证，模拟器 Debug 构建成功，阅读位置 4 项单元测试通过。
- 测试运行中修复 ReadingPositionStoreTests 的可选 CGFloat 断言与 throws 声明，并为 ArticleScrollObserver 增加最多 8 次主线程层级挂载重试；UI 测试查询改为按标识符匹配任意辅助功能元素以兼容 Xcode 27 类型变化。
- Xcode 27 Beta UI Test Runner 在两台独立 iPhone 17 Pro 模拟器上均出现启动卡死/异常退出，无法稳定完成端到端断言；应用本体可正常安装启动，最新代码重新构建成功。构建需禁用 SwiftPM 嵌套 Manifest/Package Sandbox 以适应当前 WorkBuddy 沙箱环境。
- 按选定的“纸上书房”方向重构正式“我的”页：新增今日日期刊头、文武线、本地阅读档案、带数量的收藏/已读分段和头部设置入口；不引入登录或稍后读模型。底部导航精简为日报/热榜/我的三栏，并更新 MeFlowUITests 覆盖新布局、三栏导航与设置跳转。
- XcodeGen、pbxproj 校验、Swift 语法解析和 diff check 通过；本轮 xcodebuild 在解析 Alamofire Manifest 时持续被 Xcode 27 Beta 的 sandbox-exec `sandbox_apply: Operation not permitted` 阻断，未进入项目源码编译，因此新增 UI 测试尚未实际执行。
- 完成文章正文长标题导航专项设计：确认系统 inline navigation title 的单行有限宽度是截断根因，并产出「题随文隐」「双层报眉」「墨迹岛」三套静态/动态方案及交互原型。推荐「题随文隐」并加入点击紧凑标题查看全文，后续可直接复用 ArticleScrollObserver 的 offset 实现 64pt 进入、32pt 退出的迟滞折叠状态。
- 将推荐方案深化为 V2 高保真交互：导航采用 48pt/弹性标题/48pt 均衡三槽；补齐滚动标题交接、右侧菜单、收藏与撤销反馈、全局“纸面压印”按钮状态及作者署名。现有 ArticleDetail/StorySummary 无作者字段，落地应从 HTML 的 author/avatar/meta/source 节点提取并回传原生层，失败时仅显示来源与“日”字占位。
- 基于实际问答正文截图将方案修订为 V3：原生层仅保留导航与系统能力，标题、真实作者头像、链接、问答、引用、图片和图注由单一可滚动 WKWebView 连续承载。不得通过 opacity/transform 搬走正文标题或在原生层重复固定封面；有头像保留，无头像折叠槽位。建议移除外层 ScrollView、WebView 禁止滚动与 `max(measuredHeight, 520)` 动态高度架构，直接监听 WebView scrollView 完成进度和位置恢复。
- 确定文章详情采用双正文体系：问答式「问答长卷」保持普通滚动导航标题；标题加正文的文章式采用「墨迹岛」标题胶囊与朱砂阅读进度。两类共享单一 WKWebView、纸面压印按钮、操作菜单、头像条件渲染、图片浏览与安全链接路由；模板优先用显式 ArticlePresentationStyle，DOM 识别仅作 fallback。
- 完成双正文系统增量 PRD 与文件级架构设计，文档位于 `docs/article-detail-dual-style-prd.md` 和 `docs/article-detail-dual-style-architecture.md`。架构确定六类结构化 Web 消息、单一 WKWebView 滚动事实源、qa/article/fallback 决策锁定、墨迹岛状态机、安全链接路由和 v2 阅读位置恢复。
- 已完成文章详情双正文 P0 源码实现：详情页移除外层 ScrollView、原生重复标题/题图、WebView 测高与 520pt 最低高度；新增模板分类、结构化消息桥、DOM 文档构建/净化、普通标题与墨迹岛、无边框纸面压印 Bar Item、直接 WebView 滚动观察、图片 alt 预览和站内/站外/危险链接路由。XcodeGen、全量 App Swift 类型检查、纯逻辑冒烟和 diff check 通过；Xcode 27 Beta 仍在 Alamofire Package.swift 解析阶段被嵌套 sandbox_apply 阻塞，未能执行 xcodebuild/XCTest/UI 自动化，发布前需在非嵌套沙箱环境补跑。
- 根据真实“瞎扯”问答运行截图修订双正文 HTML 设计稿：问答式改为标题、VOL、专题标题、知乎日报来源、重复 Q 链接、@回答者、靛蓝竖线与原序图片的真实层级。分类器新增无语义 class 场景下的「Q：链接 → @回答者」重复序列特征，并以强 DOM / 真实序列双轨评分；标题关键词不参与单独定案，低置信度仍 fallback 且每文档只锁定一次。新增真实序列与单个问句文章边界测试，Swift 解析和 diff check 通过。
- 只读调查 13:34 代码恢复点：Git 提交、reflog 与本地 Time Machine 均无该时刻快照；三个 stash 均创建于 20:29–20:53。当前最接近且可验证的恢复候选为 `421e008 + stash@{0}`，其保留早期作者署名栏、首字下沉、阅读进度/回顶实现，不包含后续双正文架构、墨迹岛、单 WebView 重构及“我的”页三栏导航改造；尚未执行任何代码回退。
- 按用户澄清仅撤销“最近一次正文设计”的代码：先创建 `safety-before-article-detail-restore-2026-07-28` 完整 stash，再将 ArticleDetailView、HTMLWebView、对应 fixture/测试与 DESIGN 规范恢复到此前作者署名栏、首字下沉、阅读进度/回顶版本；移除双正文、墨迹岛、结构化 Web 桥和阅读位置等后续源码，同时保留“纸上书房”我的页、三栏导航以及所有 PRD/架构/设计输出。XcodeGen、Swift 解析、diff check 与 iOS Simulator Debug 构建均通过。
- 完成文章正文“回到顶部 + 阅读进度”交互：详情页滚动超过 200pt 后在右下角显示 54pt 纸墨风进度环按钮，实时显示百分比，点击平滑回顶并自动隐藏，兼容“减少动态效果”与无障碍描述。为解决外层 SwiftUI ScrollView 混排禁滚 WKWebView 时几何偏好失效，改用挂载外层 UIScrollView 的 KVO 观察器同步 offset/contentSize/bounds；新增阈值单测并更新长正文 UI 测试。最终 ArticleDetailViewModelTests 12 项与长正文端到端场景全部通过，diff check 通过。
- 子上新增长期 Git 规则：每次完成代码修改并通过必要验证后，默认创建 Git 提交，除非明确要求暂不提交。本次回顶功能已补交为 `2ebf06c feat: 添加正文回顶与滚动进度`。
- 为日报、热榜、我的三个 NavigationStack 统一接入 UIKit 原生左边缘交互式右滑返回，不添加会抢占 List/WKWebView 的普通 DragGesture；新增详情页左缘右滑返回首页 UI 用例。XcodeGen、pbxproj 校验、桥接代码 iOS 类型检查、Swift 语法解析与 diff check 通过；完整 xcodebuild 仍被 Xcode 27 Beta 解析 Alamofire Manifest 时的嵌套 `sandbox_apply` 阻断，未执行该 UI 用例。
