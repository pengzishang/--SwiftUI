# 2026-07-29

- 为五层网络架构迁移创建 `feature/layered-network-architecture`，从 `antigravity/4-tab-update` 的干净提交 `d3d6080` 分出。
- 冻结 Endpoint、HTTPClient、Service、Repository 与 HomeFeed 公共协议并提交 `1b4c458`。
- 建立 5 个独立 Git worktree/并行分支，分别承载 Transport、Service、Repository、Home/Detail、HotList/Answers 工作轨；主工作区作为唯一集成点。
- 当前 WorkBuddy subagent API 持续返回 403，改用本机非交互代理在隔离 worktree 中并行执行。
- Xcode 27 Beta 的 SwiftPM 依赖解析受嵌套 `sandbox-exec` 限制，`xcodebuild` 在 Alamofire Package.swift 解析阶段失败；XcodeGen 可正常生成工程，最终需明确验证边界。
- 完成五层网络架构全量迁移：typed Endpoint/HTTPClient、ZhihuDailyService/LocalFixtureDailyService、actor DailyRepository、Home/Detail/HotList/Answers ViewModel Repository 注入与 AppEnvironment 组合根；删除旧 DailyAPIClient/ZhihuDailyAPI/LocalFixtureDailyAPIClient。
- 新增 Endpoint、HTTPClient、Service、Repository 测试并迁移现有 ViewModel 测试。应用 Target 曾成功构建一次；最终源码与测试全部通过 Swift frontend 语法解析，`git diff --check` 通过；后续完整 xcodebuild/test 重跑仍受 SwiftPM 内层 sandbox-exec 阻塞。
- 完成 Kingfisher 8 原生图片第一阶段接入：新增集中式图片缓存服务（64 MB 内存/1 小时、300 MB 磁盘/7 天）和 `RemoteImageView`，统一 Home、Detail、HotList 及全屏预览图片，支持目标尺寸降采样、缓存、重试、取消、HTTP(S)/base64 分流；正文 `WKWebView` 图片继续留待第二阶段。
- Kingfisher 集成通过 XcodeGen、全量 Swift 语法解析、架构扫描、`git diff --check`，并用 Kingfisher 8.0.0 官方源码直接编译 iOS Simulator 模块后完成图片基础设施 API 类型核验；完整 xcodebuild/test 仍在解析既有 Alamofire 包清单时受 Xcode 27 Beta `sandbox-exec` 限制阻断。
- 在 `fix/history-pagination-jump` 完成历史分页优化并提交 `c2fabeb`：历史日报永久缓存并支持批量读取，每批缓存优先、仅并发请求最多 10 个缺失日期；独立连续游标避免空日/已读日重复请求，ViewModel 最多连续扫描 12 批直至出现可见未读内容；移除 footer 重建任务循环，历史缓存/失败不再污染整页离线提示。XcodeGen、目标源码语法解析、Repository/缓存/HomeViewModel 类型检查及 `git diff --check` 通过；完整构建仍受 Xcode 27 Beta SwiftPM `sandbox-exec` 限制阻断。
- 历史分页触发点进一步提前：可见列表还剩 8 条时即开始预取，短列表从首条出现便触发，不再等待用户滑到底部；提交 `19efa37`。
- 完成文章互动数据 UI 设计：输出 `outputs/article-metrics-design.html` 与设计规范；首页仅显示日报热度/评论，正文按“日报数据/原回答”分行，异步加载静默显现、失败或无数据整组隐藏，不落盘或跨页面缓存。当前仅设计，未修改 SwiftUI 源码。
- 在 `feature/article-metrics-ui` 完成完整试做：新增 story-extra 与唯一原回答指标链路，首页仅在日报列表行异步显示热度/评论，详情按来源显示日报和原回答指标；无数据、失败、多原回答时隐藏，不写入持久缓存。保留现有列表/详情核心布局，并在 iOS 27 模拟器验证首页、详情、无数据、深色和无障碍大字号状态；应用目标完整类型检查与源码解析通过，常规 xcodebuild/test 仍受 Xcode 27 Beta SwiftPM 嵌套沙盒阻塞。
- 根据实机截图微调首页指标节奏：用自定义 `HStack(spacing: 3)` 替代 `Label` 收紧图标与文字，并将标题到题下信息行间距从 7pt 增至 10pt；重新类型检查和模拟器截图验证通过。
- 按第二轮视觉反馈将首页热度/评论固定到独立指标行，移除与 `hint` 同排的 `ViewThatFits` 和中点分隔；`hint` 到指标上距 8pt、无 `hint` 时标题到指标 14pt。完整语法解析、Simulator 类型检查和真实模拟器截图验证通过。
- 完成 AI 文章问答与多 Session 的完整 UI 规格，输出 `outputs/ai-chat-ui-design-spec.md` 和交付概览；设计沿用“今日刊”纸墨体系，覆盖首页/正文入口、全屏对话、Session 列表、文章上下文、AI 配置、状态与 WCAG 2.1 AA，并定义 <480/≥480/≥768/≥1200 四档适配。XcodeGen 成功；当前 xcodebuild 仍在 Alamofire 依赖解析阶段受 Xcode 27 Beta `sandbox-exec` 限制阻断，未进入 Swift 源码编译。
- 完成 AI 阅读助手可视化 UI 设计图，输出 `outputs/ai-chat-visual-design/index.html` 与 1510×1600 PNG 总览，覆盖正文选词/悬浮入口、文章上下文对话与真实来源、Session Sheet、首页独立空态、Endpoint 配置五个核心手机界面；沿用“今日刊”纸墨、宋体、靛蓝与髮丝线体系，主文本及辅助文本对比度核验均达到 WCAG AA。
