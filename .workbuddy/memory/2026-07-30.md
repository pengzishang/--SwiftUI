# 2026-07-30

- 在 `feature/ai-reading-assistant` 完成 AI 阅读助手实现：正文 `WKWebView` 全文可选与原生“AI 搜索”菜单、选词预填但不自动发送、文章上下文快照、首页和正文入口、全屏多 Session 对话、会话新建/切换/重命名/删除、上下文冲突确认、流式输出/停止/重试/复制、搜索状态与来源引用。
- 新增 OpenAI 兼容 Endpoint/模型配置，Endpoint 仅接受 HTTPS；API Key 独立保存在 Keychain，会话原子持久化至 Application Support，启动时将未完成流恢复为中断状态，并兼容旧 ISO-8601 日期文件。
- 新增文章上下文预算与清洗、SSE 解析、会话持久化、重试和启动合并测试；修复既有异步 XCTest autoclosure 编译问题，并为 `HomeViewModelTests` 同步清理 UserDefaults/Keychain，消除模拟器持久状态污染。
- 验证结果：arm64 iOS 27 模拟器应用构建成功；AI 聚焦测试 12/12 通过；完整 `DailyReaderTests` 101/101 通过；应用已安装并成功启动；`git diff --check` 与工程 `plutil` 校验通过。旧 UI 自动化仍依赖已过期首页固定文案，未作为本次 AI 功能提交门槛；原生 WebKit 长按菜单和真实自定义 Endpoint 仍需人工端到端验证。
- 使用 SenseNova OpenAI 兼容测试服务和 `deepseek-v4-flash` 完成真实协议验收（密钥未写入仓库）：非流式、SSE、多轮及约 2.1 万字文章上下文均成功；极短请求出现约 1.3–12.4 秒双峰延迟。该模型大量输出 `reasoning_content`，App 当前 `max_tokens: 8` 的连接测试会得到空 `content`/`length`，需提高 token 预算或放宽成功判定；未返回 `tool_calls`、`citations` 或 `sources`，不能视为具备实时搜索与结构化引用能力。
- 完成多 Provider 并行竞速：支持内置与用户服务排序/启停、首个非空正文获胜并取消其余请求、reasoning 仅显示通用思考状态、胜者标记、全失败聚合、全部关闭配置引导、构建时 Base64URL 注入及 Provider 级 Keychain；归一化 Endpoint/模型/凭据去重，并清理已移除内置服务的遗留凭据。最终验证为竞速边界 6/6、取消稳定性重复 10/10、完整单元测试 117/117、新增无 Provider UI 自动化 1/1，最新应用已安装并启动；仓库敏感信息扫描无命中。真实三服务并行验收因本机未保留凭据，留待安全重新注入后执行。
- 将三个内置 Provider 重构为一个可见的“默认服务”和三条内部 lane，完成 `DailyReader.ai.providers.v2` 到 v3 的折叠迁移、lane 级 Keychain 凭据保留/清理、逻辑服务排序启停、连接测试复用首个非空白 `delta.content` 竞速，以及生产 UI 隐藏 lane 身份；Debug 仅记录赢家 lane ID。验证结果：AI 聚焦测试 24/24、完整单元测试 125/125、AI 设置相关 UI 测试 2/2、Release 模拟器构建均通过；最新 Debug 应用已安装并冷启动。敏感信息扫描无真实密钥命中，提交为 `b5095f3 feat: unify built-in AI service racing`。真实三 lane 在线验收仍因无法安全恢复原始密钥而阻塞。
- 新增 `Config/AIProviders.payload.json.example`，提供一个默认服务下 `online`、`sensenova_gou`、`eric` 三条 lane 的可直接照填 JSON 模板；模板仅含占位值，不包含真实凭据。
- 完成三条真实 SenseNova lane 的最终验收：`online`、`sensenova_gou`、`eric` 均能独立产生非空白 SSE `delta.content`，并发竞速由 `eric` 首先产生正文、其余 2 条请求随即取消。使用本地忽略配置重建 Debug App 成功，构建产物确认仅暴露一个 `builtin.default` / “默认服务”且包含三条独立内部 lane，已安装并冷启动到 iOS 27.0 模拟器。最终安全复核确认本地配置未跟踪、权限 `0600`、tracked 文件无真实密钥命中、临时明文文件均已清理。
- 修复 iOS 27 AI 对话顶部导航栏标题被操作胶囊严重挤压：右侧只保留一个 44pt “更多”菜单，将“新建对话”移入菜单首项；随后根据实机截图进一步将标题容器限制为视口宽度的 52%（150–230pt）并强制裁切，确保长标题不会延伸到左右按钮下方。Debug 模拟器构建通过，新版本已安装并启动。
- 完成 2.0 发布：应用版本升级为 `2.0 (2)`，文档按 `releases/features/design/testing/archive` 归类并新增 2.0 发布说明；修复 UI 自动化读取真实内置 AI 凭据的隔离问题，使测试模式使用独立 UserDefaults 与空凭据存储。最终验证为单元测试 125/125、AI UI 自动化 2/2、Release 模拟器构建与冷启动成功，文档链接、Plist、格式及敏感信息扫描通过；发布内容已快进合并到 `main` 并创建 `2.0.0` 标签。
- 将 `main` 推送到 `origin/main`。因未推送历史中的 `a8ad42d` 曾误提交约 400 MiB DerivedData/模块缓存，HTTPS 持续中断；经用户确认，先以 `backup/pre-squash-2.0-b829d23` 保留完整原历史，再将远端 `a586a8e` 之后压缩为干净发布提交。压缩前后 Git tree 哈希一致，最终源码与文档未改变；远端 `main` 已更新到 `f434bac`。
